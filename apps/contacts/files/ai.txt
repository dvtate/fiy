using LLMs for frontend bc I suck at it

keeping this here for now

Prompt:
Write the html+css+js for a contacts webpage.

- The page should have a search bar
- The page should have a list of contacts.
- On desktop the user should see a list of contacts on the left view contacts on the right
- On mobile the user should see a list of contacts, when a contact is selected the view is replaced with the contact details

